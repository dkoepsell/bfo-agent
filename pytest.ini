[pytest]
# Only the live suite. deploy/dgx_staging/ holds verbatim copies of tests
# staged for the DGX fork; collecting them from the repo root double-counts
# and errors on their relative imports.
testpaths = tests
